import PageWrapper from "../src/components/PageWrapper";

export default function FocusAreasPage() {
  return (
    <PageWrapper
      {...{ mainTitle: "Focus Areas", category: "Research" }}
    ></PageWrapper>
  );
}
